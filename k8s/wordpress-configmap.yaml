apiVersion: v1
kind: ConfigMap
metadata:
  name: wordpress-config
  namespace: wordpress
data:
  # Example WordPress configuration values. Use as needed or extend.
  WP_TABLE_PREFIX: wp_
  WP_DEBUG: "false"
